// material-ui
import { styled, Theme } from '@mui/material/styles';

// ==============================|| AUTHENTICATION 1 WRAPPER ||============================== //

const AuthWrapper = styled('div')(({ theme }: { theme: Theme }) => ({
    minHeight: '100%',
    width: "100%"
}));

export default AuthWrapper;
